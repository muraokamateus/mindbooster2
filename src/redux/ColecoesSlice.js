import { createSlice } from "@reduxjs/toolkit";

export const ColecoesSlice = createSlice({
    name: 'ColecoesS',
    initialState: [],
    reducer: {
        addColecoes:(state,action) =>{
            
        },
        changeColecoes:(state,action) =>{

        },
        deleteColecoes:(state,action) =>{

        }

    }
});

export const {} = ColecoesSlice.actions;
